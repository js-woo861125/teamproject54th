<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="ks54team01.customer.member.mapper.MemberMapper">
  	<resultMap type="CustomerMember" id="corpMemberResultMap">
		<!-- id : 테이블 pk 컬럼 -->
		<id 	column="members_id" 				property="memberId" />
		<!-- result : 테이블 일반 컬럼 -->
		<result column="members_pw" 				property="memberPw" />
		<result column="members_type" 				property="memberType" />
		<result column="state_transition_date" 		property="memberStateTransitionDate" />
		<result column="cust_nm" 					property="custName" />
		<result column="cust_brdt" 					property="custBrdt" />
		<result column="cust_addr" 					property="custAddr" />
		<result column="cust_daddr" 				property="custDaddr" />
		<result column="cust_email" 				property="custEmail" />
		<result column="cust_phone" 				property="custPhone" />
		<result column="corp_brno" 					property="corpBrno" />
		<result column="corp_nm" 					property="corpName" />
	</resultMap>

  	<resultMap type="CommonMember" id="commonMemberResultMap">
		<!-- id : 테이블 pk 컬럼 -->
		<id 	column="members_id" 				property="memberId" />
		<!-- result : 테이블 일반 컬럼 -->
		<result column="members_pw" 				property="memberPw" />
		<result column="members_type" 				property="memberType" />
		<result column="state_transition_date" 		property="memberStateTransitionDate" />
	</resultMap>
	
  	<update id="modifyCorpInfo" parameterType="CustomerMember">
  		/* 회원정보 수정 */
		UPDATE corp_customer
		<set>
			<if test="corpBrno != null and corpBrno != ''">
				corp_brno = #{corpBrno}, 			
			</if>
			<if test="corpName != null and corpName != ''">
				corp_nm = #{corpName}, 			
			</if>
				revision_date = CURDATE()
		</set>		 
		WHERE 
			cust_id = #{memberId};
  	</update>
  	
  	<update id="modifyCustomerInfo" parameterType="CustomerMember">
  		/* 회원정보 수정 */
		UPDATE customer
		<set>
			<if test="custName != null and custName != ''">
				cust_nm = #{custName}, 			
			</if>
			<if test="custBrdt != null and custBrdt != ''">
				cust_brdt = #{custBrdt}, 			
			</if>
			<if test="custAddr != null and custAddr != ''">
				cust_addr = #{custAddr}, 			
			</if>
			<if test="custDaddr != null and custDaddr != ''">
				cust_daddr = #{custDaddr}, 			
			</if>
			<if test="custEmail != null and custEmail != ''">
				cust_email = #{custEmail}, 			
			</if>
			<if test="custPhone != null and custPhone != ''">
				cust_phone = #{custPhone}, 			
			</if>
				revision_date = CURDATE()	
		</set>		 
		WHERE 
			cust_id = #{memberId};
  	</update>
  	
  	<update id="modifyCommonInfo" parameterType="map">
  		/* 회원 공통정보 수정 */
		UPDATE members
		<set>
			<if test="newPw != null and newPw != ''">
				members_pw = #{newPw},
			</if>
				state_transition_date = CURDATE()	
		</set>		 
		WHERE 
			members_id = #{memberId};
  	</update>
  
	<select id="isPwCheck" parameterType="map" resultType="boolean">
		/* 비밀번호 일치여부 체크 */
		SELECT 
			COUNT(*)
    	FROM 
    		members
    	WHERE 
    		members_id = #{memberId} 
    		AND members_pw = #{memberPw};
	</select>
  
	<select id="getCorpInfoById" resultType="ks54team01.customer.member.domain.CustomerMember">
		/* 기업고객 정보 조회 */
	    SELECT 
	        memberId,
	        memberPw,
	        memberType,
	        corpName,
	        corpBrno,
	        custName,
	        custPhone,
	        custBrdt,
	        custAddr,
	        custDaddr,
	        custEmail
	    FROM 
	    	corp_customer_info_view
	    WHERE 
	    	memberId = #{memberId}
	</select>
	
	<select id="getCustomerInfoById" resultType="ks54team01.customer.member.domain.CustomerMember">
		/* 개인 및 기업고객 정보 조회 */
	    SELECT 
	        memberId,
	        memberPw,
	        memberType,
	        custName,
	        custPhone,
	        custBrdt,
	        custAddr,
	        custDaddr,
	        custEmail
	    FROM 
	    	customer_info_view
	    WHERE 
	    	memberId = #{memberId}
	</select>
	
  </mapper>